Folder with the weights learned by the SARSA agents.
The folder good_weights/ contains the weights of some trained SARSA agents and is used for greedy behaviour 
(e.g., evaluating SARSA agents, obtaining the fidelity score wrt SARSA agents, etc).